# Required in production. Generate with: openssl rand -base64 32
# Used to sign OAuth 2.1 access tokens (HS256). Production boot fails fast
# if this is missing or shorter than 32 bytes (see server/lib/oauth/jwt.ts
# assertSecretReady).
OAUTH_JWT_SECRET=
